import { Box } from "@mui/system";
import React from "react";

const HotelListingFilter = () => {
  return (
    <Box
      sx={{ position: "sticky", top: 50, left: 0, backgroundColor: "yellow" }}
      height="70vh"
      minWidth={350}
    ></Box>
  );
};

export default HotelListingFilter;
